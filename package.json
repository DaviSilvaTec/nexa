{
  "name": "alltec-nexa",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "test": "npm run build && node --test dist/test",
    "test:watch": "npm run build && node --test --watch dist/test"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@fastify/static": "^9.1.0",
    "fastify": "^5.8.4"
  }
}
